DELIMITER //

CREATE PROCEDURE tampil_genap()
BEGIN
 	DECLARE i INT DEFAULT 2;
    
 WHILE i <= 20 DO
 	SELECT i AS bilangan_genap;
 	SET i = i + 2;
 END WHILE;
END //

DELIMITER ;

CALL tampil_genap();